import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'apresentacao-segura-tech',
  webDir: 'www'
};

export default config;
